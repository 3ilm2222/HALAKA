# خطة تصميم واجهات «سجلّ الطالب القرآني»

## توجه التصميم

صُمّم التطبيق للهواتف في وضع عمودي **9:16** وبواجهة عربية من اليمين إلى اليسار، بحيث يستطيع المعلم إنجاز المتابعة اليومية بيد واحدة، بينما يصل وليّ الأمر إلى سجلّ ابنه بوضوح ومن دون أدوات تعديل. تعتمد الهوية على الأخضر القرآني الهادئ والورق الدافئ، مع بطاقات واسعة، نص عالي التباين، وأزرار رئيسية بارزة في مواضع إبهام اليد.

## ألوان الهوية

| الاستخدام | اللون | الرمز |
| --- | --- | --- |
| اللون الأساسي | أخضر زمردي قرآني | `#176B53` |
| اللون الثانوي | ذهبي هادئ للتقدم والتمييز | `#C8902F` |
| الخلفية | عاجي ورقي دافئ | `#F8F6F0` |
| البطاقات | أبيض | `#FFFFFF` |
| النص الرئيسي | فحمي داكن | `#21332D` |
| النص الثانوي | أخضر رمادي | `#5D7069` |
| التنبيه | خمري هادئ | `#A63D40` |

## قائمة الشاشات

| الشاشة | المحتوى الأساسي | الوظائف المتاحة |
| --- | --- | --- |
| بوابة البداية | تعريف موجز وخيار «دخول المعلم» أو «دخول ولي الأمر» | توجيه المستخدم إلى المسار المناسب.
| قائمة الطلاب للمعلم | بحث، بطاقات الطلاب، زر إضافة طالب، مؤشرات الملاحظات غير المقروءة | إضافة وتعديل وحذف الطلاب، فتح ملف طالب، الانتقال إلى الرسائل.
| نموذج الطالب | الاسم والعمر والرمز السري المخصص لولي الأمر | إنشاء الطالب أو تحديث بياناته، مع إظهار/إخفاء الرمز السري بأمان.
| ملف الطالب للمعلم | هوية الطالب، شريط الأشهر، السبورة الشهرية، حقل ملاحظة لولي الأمر | تحرير السبورة الحالية، استعراض الأشهر المؤرشفة، إضافة شهر، إرسال ملاحظة وتنبيه.
| السبورة الشهرية | سطح رسم كبير، أدوات قلم وممحاة ونص وأشكال وإطار وصورة وتراجع/إعادة | إضافة وعرض وتعديل عناصر السبورة وحفظها في سجل الشهر المحدد.
| أرشيف الأشهر | شرائح أشهر مرتبة زمنياً مع حالة «الحالي» و«مؤرشف» | فتح شهر سابق للعرض أو إنشاء شهر جديد للمعلم.
| دخول ولي الأمر | اسم الطالب والرمز السري وخيار البقاء مسجلاً | التحقق من بيانات الطالب وحفظ الجلسة محلياً على الجهاز.
| ملف الطالب لولي الأمر | الاسم والعمر والشهر المختار والسبورة في وضع العرض فقط | رؤية محتوى الشهر بلا أدوات تحرير، تغيير الشهر، إرسال رسالة للمعلم.
| الرسائل والتنبيهات | سجل تواصل خاص بكل طالب ورسائل المعلم وولي الأمر | إرسال رسالة، إظهار حالة الإرسال والرسائل غير المقروءة.

## تدفقات الاستخدام الرئيسية

### تدفق المعلم

يدخل المعلم إلى قائمة الطلاب، ثم يضغط «إضافة طالب» ويملأ الاسم والعمر والرمز السري. عند حفظ البطاقة، يفتح ملف الطالب ويختار الشهر الحالي. يكتب المعلم، يرسم، يضيف مربعات أو إطارات أو صوراً على السبورة. تحفظ التعديلات تلقائياً في سجل الشهر الحالي. عند انتهاء الشهر، يبقى المحتوى مؤرشفاً، ويبدأ الشهر الجديد بسبورة فارغة عبر خيار «إضافة شهر» أو الإنشاء التلقائي عند أول تحرير.

### تدفق ولي الأمر

يختار ولي الأمر «دخول ولي الأمر»، ثم يدخل اسم الطالب والرمز السري. بعد نجاح التحقق يمكنه إبقاء الجلسة محفوظة على جهازه. تظهر له سبورة الطالب للشهر الحالي في وضع قراءة فقط؛ فلا تظهر أي أدوات رسم أو تعديل. يستطيع فتح الأشهر السابقة وكتابة رسالة قصيرة للمعلم من الحقل الثابت أسفل الصفحة.

### تدفق الملاحظة والتنبيه

يكتب المعلم ملاحظة في ملف الطالب ثم يضغط «إرسال لولي الأمر». تُحفظ الملاحظة في سجل التواصل، ويُنشأ تنبيه موجّه إلى الجهاز المسجّل لولي الأمر. عند فتح التطبيق يظهر ملخص الملاحظة، ثم يفتح ولي الأمر ملف الطالب أو سجل الرسائل لقراءتها كاملة.

## نموذج البيانات

| الكيان | الحقول الرئيسية | الغرض |
| --- | --- | --- |
| Student | `id`، `name`، `age`، `parentPin`، `createdAt` | بطاقة الطالب وبيانات الدخول لولي الأمر.
| MonthlyBoard | `id`، `studentId`، `monthKey`، `label`، `elements`، `updatedAt` | سجل سبورة طالب واحد لشهر محدد.
| BoardElement | `id`، `type`، `x`، `y`، `width`، `height`، `style`، `content` | قلم أو نص أو شكل أو إطار أو صورة قابلة لإعادة العرض.
| ConversationMessage | `id`، `studentId`، `senderRole`، `content`، `createdAt`، `readAt` | قناة مراسلة واحدة بين المعلم وولي أمر كل طالب.
| ParentSession | `studentId`، `deviceToken`، `rememberedAt` | ربط جلسة ولي الأمر المحفوظة بجهازه لتنبيهه.

## قواعد الصلاحيات

| الدور | الصلاحيات |
| --- | --- |
| المعلم | إدارة الطلاب، إنشاء الأشهر، تحرير السبورة، إرسال الملاحظات، قراءة وإرسال الرسائل.
| ولي الأمر | عرض ملف الطالب وسبورات الأشهر فقط، تغيير الشهر، قراءة وإرسال الرسائل؛ ولا يملك أي صلاحية لإضافة أو تعديل أو حذف عناصر السبورة.

## قرارات تجربة الاستخدام

تظهر أدوات السبورة في شريط سفلي قابل للتمرير مع زر إجراء دائري للخيارات السريعة، وتبقى مساحة السبورة أكبر عنصر مرئي في الشاشة. يعرض شريط الأشهر أعلى السبورة ليتطابق مع نموذج الدفتر المدرسي. تستعمل العمليات المهمة، مثل حذف طالب أو مسح عنصر، نافذة تأكيد واضحة. تُحفظ جلسة ولي الأمر في التخزين الآمن على جهازه، ويمكنه تسجيل الخروج من صفحة الإعدادات لاحقاً.
