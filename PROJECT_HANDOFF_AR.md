# وثيقة تسليم مشروع «سجل الطالب القرآني»

> هذه الوثيقة موجهة إلى أي مطور أو نظام ذكاء اصطناعي سيواصل تطوير المشروع. وهي تصف البنية الحالية والوظائف ومسارات البيانات وملفات المشروع وطريقة التشغيل والبناء.

## 1. تعريف المشروع

«سجل الطالب القرآني» تطبيق Expo/React Native باللغة العربية وباتجاه RTL لإدارة طلاب المدرسة القرآنية. يحتوي على بوابتين منفصلتين: بوابة المعلم لإدارة الطلاب والسبورات الشهرية والغياب والرسائل والأخبار، وبوابة ولي الأمر لعرض تقدم الطالب والتواصل مع المعلم.

التطبيق مصمم ليعمل بأسلوب **Offline-First للمعلم**. يستطيع المعلم فتح قائمته وتعديل البيانات أثناء انقطاع الإنترنت، وتحفظ التعديلات على الجهاز داخل طابور محلي، ثم تُرفع إلى Supabase عند عودة الاتصال. أما واجهة ولي الأمر فهي سحابية للقراءة وتحتاج إلى اتصال لعرض آخر بيانات المعلم.

التقنيات الأساسية هي Expo SDK 54، React Native 0.81، React 19، Expo Router، TypeScript، NativeWind، React Native Gesture Handler، Supabase Edge Functions، PostgreSQL/Supabase، AsyncStorage وSecureStore.

## 2. طريقة تشغيل المشروع

يستخدم المشروع مدير الحزم pnpm والإصدار المحدد في `package.json` هو pnpm 9.12.0. الأوامر الرئيسية هي:

| الأمر | الغرض |
|---|---|
| `pnpm install` | تثبيت الاعتماديات وفق `pnpm-lock.yaml` |
| `pnpm dev` | تشغيل خادم التطوير وMetro معاً |
| `pnpm check` | فحص TypeScript دون إخراج ملفات |
| `pnpm lint` | تشغيل Expo ESLint |
| `pnpm test` | تشغيل اختبارات Vitest |
| `pnpm build` | تجميع خادم Node في مجلد `dist` |
| `pnpm android` | تشغيل Expo مع Android محلياً |
| `pnpm qr` | إنشاء QR وفق سكربت المشروع |

يفضل قبل أي تعديل تشغيل `pnpm check && pnpm test`، ثم تشغيل `git diff --check` للتأكد من عدم وجود أخطاء مسافات أو أسطر.

## 3. المسارات والشاشات

| الملف | الوظيفة الحالية |
|---|---|
| `app/index.tsx` | نقطة البداية، وتفتح بوابة ولي الأمر مباشرة. |
| `app/parent-cloud.tsx` | البوابة الأساسية لولي الأمر: تسجيل الدخول، عرض السبورة، الغياب، الأخبار، الرسائل، التحديث والخروج. |
| `app/parent-login.tsx` | مسار قديم لتسجيل ولي الأمر يعتمد على التدفق القديم؛ ليس المسار الافتراضي الحالي. لا تحذفه دون التأكد من عدم وجود روابط تعتمد عليه. |
| `app/parent-board.tsx` | شاشة قديمة/بديلة لعرض لوحة ولي الأمر. |
| `app/teacher/login.tsx` | شاشة إدخال رمز المعلم. تدعم الدخول السحابي، والدخول المحلي بعد أول تحقق سحابي ناجح. |
| `app/teacher/cloud.tsx` | قائمة المعلم السحابية والمحلية، البحث، الجاهزية للتسميع، الغياب، الأخبار، الجرس والتحديث. |
| `app/teacher/cloud-student.tsx` | الصفحة الفعلية لملف الطالب، وتضم السبورة الشهرية، الحفظ التلقائي، الرسائل، التنبيهات والإعدادات. |
| `app/teacher/student/[id].tsx` | مسار ديناميكي مرتبط بصفحة الطالب؛ افحص الاعتماد عليه قبل إعادة هيكلة المسارات. |
| `app/teacher/index.tsx` | واجهة معلم قديمة/احتياطية تعتمد على التدفق المحلي القديم. |
| `app/_layout.tsx` | تخطيط الجذر، Safe Area، Gesture Handler، ThemeProvider، React Query، tRPC وStack. |
| `app/oauth/callback.tsx` | مسار callback لتدفق OAuth الموجود في القالب. |
| `app/dev/theme-lab.tsx` | شاشة تطوير لاختبار ألوان وثيمات التطبيق. |
| `app/(tabs)/index.tsx` و`app/(tabs)/_layout.tsx` | ملفات القالب الأصلي، وليست واجهة الاستخدام الرئيسية الحالية. |

## 4. مكوّنات الواجهة المهمة

| الملف | المسؤولية |
|---|---|
| `components/board-canvas.tsx` | قلب السبورة: الرسم، النص، الأشكال، الصور، التحريك، التحديد، التمرير، السطور المنظمة ووضع العرض فقط. يجب الحذر عند تعديل الإحداثيات بسبب RTL. |
| `components/app-ui.tsx` | الأزرار والحقول والبطاقات والألوان المشتركة، ومنها `PrimaryButton` و`FormField` و`Surface`. |
| `components/screen-container.tsx` | حاوية Safe Area وخلفية الشاشة. استخدمها للشاشات الجديدة. |
| `components/ui/icon-symbol.tsx` | ربط أسماء الأيقونات بأيقونات Material Icons. أضف أي اسم جديد إلى الخريطة قبل استعماله. |
| `components/ui/icon-symbol.ios.tsx` | نسخة iOS من مكوّن الأيقونات. |
| `components/haptic-tab.tsx` | سلوك الضغط اللمسي للتنقل. |
| `components/themed-view.tsx` و`parallax-scroll-view.tsx` و`hello-wave.tsx` | مكونات القالب العامة، وبعضها غير أساسي للتدفق الحالي. |

## 5. نموذج بيانات السبورة

النوع الرئيسي هو `BoardElement` في `lib/app-types.ts`. كل عنصر يملك `id` و`type` وقد يملك `x`, `y`, `width`, `height`, `color`, `text`, `fontSize` وخصائص إضافية حسب نوعه.

| النوع | الاستخدام |
|---|---|
| `path` | خط أو رسم حر، ويحوي `points`. |
| `text` | نص حر قابل للتحرير والتكبير والتلوين. |
| `square` و`frame` | أشكال وإطارات. |
| `image` | صورة مضافة إلى السبورة مع `uri`. |
| `ayahRow` | سطر من آية إلى آية، مع السورة ومربع الإنجاز والتاريخ. |
| `surahTitle` | سطر مزخرف لاختيار اسم السورة. |
| `fullSurah` | سطر السورة كاملة مع مربع الإنجاز. |
| `reviewRow` | سطر المراجعة والملاحظة واختيار السورة. |
| `attendanceRow` | سطر غياب صباحي أو مسائي مؤرخ. |
| `weekRow` | سطر الأسبوع الأول إلى الرابع. |

السبورة الحالية شهرية، وتحتفظ بـ `month_key` و`canvas_height` و`theme_key` و`theme` و`elements`. في وضع ولي الأمر، تكون `BoardCanvas` غير قابلة للتحرير، ويعاد حساب عرض السطور المنظمة ليتناسب مع عرض الهاتف.

## 6. تدفق المعلم

يفتح المعلم رابط «دخول المعلم» من بوابة ولي الأمر، ثم يدخل رمز المعلم. عند وجود الإنترنت يستدعي التطبيق `teacher.login`. بعد نجاح الدخول يحفظ رمز الجلسة، ويحفظ نسخة محلية من الرمز للسماح بالدخول لاحقاً دون إنترنت. عند عدم توفر الإنترنت، يُقارن الرمز بتجزئة محفوظة محلياً في SecureStore/localStorage.

بعد الدخول تظهر قائمة الطلاب. يمكن للمعلم البحث بالاسم، تفعيل حالة «مستعد للتسميع» المؤقتة للجلسة، وتسجيل الغياب الصباحي أو المسائي مباشرة من البطاقة. الطالب الغائب صباحاً أو مساءً يتلون حسب الحالة، والغائب في الفترتين يذهب إلى آخر القائمة.

من صفحة الطالب يستطيع المعلم تعديل الاسم والعمر ورمز ولي الأمر، حذف الطالب مع تأكيد، اختيار الشهر، تحرير السبورة، إرسال ملاحظة، قراءة الرسائل، حفظ خبر عام وتعديله، ومتابعة حالة الإشعارات.

## 7. تدفق ولي الأمر

التطبيق يفتح افتراضياً في `app/parent-cloud.tsx`. يدخل ولي الأمر اسم الطالب والرمز السري. بعد نجاح `parent.login` تحفظ الجلسة محلياً. يستدعي التطبيق `parent.snapshot` لعرض بيانات الطالب والسبورة والغياب والرسائل والخبر.

ولي الأمر لا يرى أدوات تحرير السبورة. يمكنه إرسال رسالة للمعلم، تحديث البيانات يدوياً، انتظار التحديث التلقائي كل دقيقة، إظهار أو إخفاء شريط الأخبار، وتسجيل الخروج بعد تأكيد.

شريط الأخبار يعرض خبراً واحداً مشتركاً محفوظاً للمعلم، ويدعم النص الطويل والتكرار والحركة. إعداد إشعارات الجهاز يجب أن يحدث بعد نجاح الدخول حتى لا يمنع شاشة الدخول، كما أن Expo Go لا يدعم الإشعارات البعيدة بصورة كاملة في SDK 53 وما بعده؛ يلزم Development Build أو APK حقيقي لاختبارها.

## 8. التخزين المحلي والمزامنة

| الملف | المسؤولية |
|---|---|
| `lib/cloud-teacher-offline.ts` | مخزن المعلم المحلي، قراءة/كتابة الطلاب والغياب والتفاصيل والصفوف المؤجلة. |
| `lib/cloud-teacher-offline-queue.ts` | تعريف طابور التعديلات وتنفيذها بالترتيب عند عودة الاتصال. |
| `lib/cloud-teacher-offline-access.ts` | حفظ تجزئة رمز المعلم والجلسة المحلية للدخول دون إنترنت. |
| `lib/cloud-teacher-session.ts` | حفظ جلسة المعلم السحابية. |
| `lib/cloud-parent-session.ts` | حفظ جلسة ولي الأمر السحابية. |
| `lib/backup-file.ts` | تصدير واستيراد نسخة JSON محلية عبر Document Picker وFileSystem والمشاركة. |
| `lib/local-date.ts` | مصدر التاريخ المحلي الموحد لتسجيل الغياب والتواريخ. |

التصميم الصحيح هو: احفظ محلياً أولاً، أضف العملية إلى الطابور، حاول الرفع عند توفر الإنترنت، ولا تحذف العملية من الطابور إلا بعد نجاح الخادم. عند فشل الرفع يجب إبقاء العملية المحلية وبيان عدد العمليات المعلقة للمعلم.

## 9. واجهة Supabase السحابية

العميل موجود في `lib/supabase-school-api.ts` ويستدعي Edge Function واحدة بعنوان `school-api`. من العمليات الحالية:

| العملية | الغرض |
|---|---|
| `teacher.status` | معرفة حالة إعداد المعلم. |
| `teacher.login` | التحقق من رمز المعلم وإنشاء جلسة. |
| `teacher.snapshot` | جلب قائمة المعلم وبياناته. |
| `teacher.detail` | جلب ملف طالب محدد. |
| `teacher.upsertStudent` | إضافة أو تعديل الطالب. |
| `teacher.deleteStudent` | حذف الطالب. |
| `teacher.toggleAttendance` و`teacher.setAttendance` | تسجيل الغياب. |
| `teacher.saveBoard` | حفظ السبورة. |
| `teacher.createMessage` و`teacher.markMessagesRead` | رسائل وملاحظات المعلم. |
| `teacher.createNews` و`teacher.updateNews` و`teacher.deleteNews` | إدارة الخبر العام. |
| `teacher.sendWeekReminder` | تذكير ولي الأمر بالأسبوع. |
| `parent.login` | دخول ولي الأمر باسم الطالب ورمزه. |
| `parent.snapshot` | جلب لقطة ولي الأمر. |
| `parent.registerPushToken` و`parent.createMessage` | تسجيل الجهاز وإرسال رسالة المعلم. |

يوجد timeout للطلبات السحابية. إذا انتهت المهلة يجب عرض رسالة واضحة، وليس ترك المستخدم في حالة تحميل بلا نهاية.

## 10. قاعدة البيانات والـ Edge Function

المسار الحي الرئيسي هو `supabase/functions/school-api/index.ts`، والترحيلات موجودة في `supabase/migrations/`:

| الكيان | دوره |
|---|---|
| `teacher_access` | حساب أو حسابات المعلم وتجزيء الرمز. |
| `teacher_sessions` | جلسات المعلم المنتهية الصلاحية. |
| `students` | الطلاب، العمر، الاسم المطبع، ورمز ولي الأمر المجزأ، مع `teacher_id`. |
| `parent_sessions` | جلسات أولياء الأمور المرتبطة بالطالب. |
| `monthly_boards` | سبورات الأشهر وعناصرها وثيماتها. |
| `attendance` | الغياب اليومي الصباحي والمسائي. |
| `messages` | رسائل المعلم وولي الأمر. |
| `teacher_news` | الخبر العام للمعلم، والمفترض حالياً خبر واحد قابل للتعديل. |
| `device_push_tokens` | رموز إشعارات أجهزة أولياء الأمور. |
| جداول التذكيرات | منع التكرار وتسجيل تذكيرات الأسبوع. |

عزل البيانات في اللقطات يتم بواسطة `teacher_id` و`student_id`. لكن توجد ملاحظة مهمة: مسار `teacher.login` الحالي يقرأ أول سجل معلم باستخدام `limit(1).maybeSingle()`. لذلك فإن إنشاء أكثر من سجل في `teacher_access` قد يؤدي إلى غموض في تحديد المعلم الذي يسجل الدخول. قبل دعم تعدد المعلمين فعلياً يجب تغيير نموذج الدخول ليستخدم معرفاً أو اسم مستخدم فريداً، ثم تعديل الجلسات وRLS والواجهات. لا تضف معلماً ثانياً إلى قاعدة البيانات باعتبار أن التطبيق يدعمه حالياً دون تنفيذ هذا التعديل.

ملفات الترحيل الحالية هي `20260826164000_initialize_quran_school.sql`، ثم ترحيلات إزالة تكرار الرسائل، تذكيرات الأسبوع، أخبار المعلم، تحديث الأخبار، وجعل خبر المعلم واحداً.

## 11. الأسرار ومتغيرات البيئة

**لا تضع كلمات السر أو مفاتيح الخدمة داخل وثيقة التسليم أو مستودع GitHub أو رسالة إلى نظام ذكاء اصطناعي.** ملف التسليم يذكر الأسماء فقط، والقيم توضع في إعدادات آمنة.

| الاسم | النوع | مكان الاستخدام |
|---|---|---|
| `EXPO_PUBLIC_SUPABASE_URL` | قيمة عامة للتطبيق | عنوان مشروع Supabase. يمكن تضمينه في APK لأنه عنوان عام، لكن لا تضع مفاتيح إدارية بجانبه. |
| `EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY` | مفتاح عميل عام | طلبات التطبيق إلى Edge Function. يجب أن يكون مفتاحاً عاماً محدود الصلاحيات، وليس Service Role Key. |
| `SUPABASE_SERVICE_ROLE_KEY` | سري جداً | Edge Function أو خادم إداري فقط، ولا يجوز وضعه في تطبيق الهاتف. |
| `SCHOOL_SETUP_KEY` | سري | إعداد حساب المعلم الأول عبر Edge Function. |
| `DATABASE_URL` أو متغيرات Drizzle | سري | المسار القديم/الخادم إذا كان مستخدماً. لا يضمّن في APK. |
| `EAS_PROJECT_ID` | إعداد بناء | مطلوب فقط لبعض وظائف Expo Push Token والبناء، وليس بديلاً عن إعداد Supabase. |

في GitHub Actions يجب إضافة أسرار المستودع باسم `EXPO_PUBLIC_SUPABASE_URL` و`EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY`. الـ Workflow الحالي يرفض البناء إذا غابت هذه الأسرار، ثم ينشئ `.env` مؤقتاً، ويتحقق من `expo.extra` قبل `expo prebuild`. لا ترفع `.env` إلى GitHub.

أي كلمة مرور أو مفتاح تم إرساله سابقاً في محادثة أو ملف غير آمن يجب اعتباره مكشوفاً، ومن الأفضل تدويره من لوحة Supabase وإنشاء قيمة جديدة قبل الاستخدام الإنتاجي.

## 12. البناء عبر GitHub Actions

الملف `.github/workflows/android-apk.yml` يبني Debug APK على خادم GitHub. المسار هو:

1. Checkout للمستودع.
2. تثبيت Node 22 وpnpm 9.12.0.
3. تثبيت الاعتماديات من `pnpm-lock.yaml`.
4. إنشاء `.env` من GitHub Secrets.
5. التحقق من Expo public config.
6. تشغيل `expo prebuild --no-install`.
7. تثبيت Java 17.
8. تشغيل `./gradlew assembleDebug`.
9. رفع `app-debug.apk` كـ Artifact لمدة 14 يوماً.

إذا كان Expo Go يعمل بينما APK لا يتصل بالسحابة، فافحص أولاً أسرار GitHub وWorkflow؛ غالباً تكون نسخة APK قد بُنيت دون إعدادات Supabase. بعد إضافة الأسرار يجب تشغيل Workflow جديد، ثم حذف APK القديم من الهاتف وتثبيت الجديد.

## 13. الاختبارات

الاختبارات في مجلد `tests/` تغطي الخروج، التاريخ المحلي، الأشهر والطلاب، طابور المزامنة، الدخول المحلي للمعلم، ترتيب الطلاب واتصال Supabase. نتيجة آخر فحص موثقة في النسخة الحالية: 29 اختباراً ناجحاً واختباران متجاوزان.

عند إضافة ميزة جديدة يجب إضافة اختبار منطق مستقل متى أمكن. لا تعتمد على اختبار المتصفح وحده لميزات Native مثل SecureStore والإشعارات؛ استخدم mocks deterministic أو اختبارات منطقية.

## 14. الملفات المساندة

| الملف/المجلد | الوصف |
|---|---|
| `theme.config.js` و`tailwind.config.js` | ألوان NativeWind والثيم العام. |
| `global.css` | توجيهات NativeWind الأساسية. |
| `constants/theme.ts` و`lib/_core/theme.ts` | لوحة الألوان runtime. |
| `lib/quran-surahs.ts` | قائمة سور القرآن. |
| `lib/board-themes.ts` | ثيمات السبورة وألوانها. |
| `lib/attendance.ts` | منطق الغياب. |
| `lib/months.ts` | الأشهر ومفاتيحها. |
| `lib/student-session-list.ts` | ترتيب الجاهزين والغائبين والبحث. |
| `lib/notifications.ts` | قناة Android وطلب صلاحية الإشعارات وتسجيل Push Token. |
| `server/cloud-backup.ts` | نسخة سحابية يومية واستعادة أحدث نسخة. |
| `server/daily-backup-schedule.ts` | جدولة النسخ اليومية في الخادم. |
| `server/routers.ts` و`server/db.ts` و`drizzle/schema.ts` | مسار Drizzle/الخادم الأقدم أو الموازي؛ المسار الحي الحالي يعتمد على Supabase. |
| `scripts/` | تحميل البيئة، إنشاء QR، تطبيق/فحص ترحيلات Supabase، وتحسين الصور. |
| `drizzle/` | ترحيلات ومخطط Drizzle القديم. لا تفترض أنه مصدر البيانات الحي قبل التحقق. |
| `assets/images/` | أيقونة التطبيق، splash، favicon وأيقونات Android. |
| `design.md` | خطة تصميم واجهة الهاتف. |
| `hosting-transition-options.md` | ملاحظات بدائل الاستضافة والانتقال. |
| `todo.md` | سجل العمل التاريخي والمهام المنجزة أو المفتوحة. |
| `app.config.ts` | اسم التطبيق، الحزمة Android، الأيقونات، الإضافات، و`extra` لإعدادات Supabase. |
| `metro.config.js` و`babel.config.js` و`tsconfig.json` | إعدادات Metro وBabel وTypeScript. |

## 15. قواعد مهمة للمطور التالي

لا تحذف أو تعيد تهيئة قاعدة البيانات لمجرد وجود مشكلة دخول قبل فحص الجلسة والمتغيرات. لا تستخدم `git reset --hard` لاسترجاع المشروع؛ استخدم نسخة محفوظة أو استرجاعاً آمناً. لا تضع Service Role Key في React Native. لا تجعل تحميل الإشعارات شرطاً لدخول المستخدم. عند تعديل السبورة اختبر وضع المعلم ووضع ولي الأمر، لأن حسابات الإحداثيات والعرض تختلف في وضع العرض وRTL.

عند ظهور «السجل يحتاج إلى اتصال» يجب التمييز بين ثلاثة أسباب: عدم وجود متغيرات Supabase في APK، انتهاء جلسة محفوظة، أو فشل فعلي في الاتصال/Edge Function. رسالة واحدة عامة تجعل التشخيص صعباً؛ يجب الحفاظ على `status` الخاص بأخطاء API وعرض رسالة مناسبة.

## 16. الحالة الحالية والمشكلات المعروفة

المشروع يحتوي على بوابتي المعلم وولي الأمر، سبورة شهرية، سطور متابعة قرآنية، غياب، أخبار، رسائل، إشعارات، تخزين محلي ومزامنة. آخر الإصلاحات ركزت على تضمين إعدادات Supabase داخل APK، مهلة طلبات الدخول، مسح جلسة ولي الأمر المنتهية، وإجبار GitHub Actions على رفض البناء بلا أسرار.

المشكلة التشغيلية التي ينبغي التحقق منها أولاً عند استلام المشروع هي اختلاف البيئات: Expo Go قد يملك متغيرات البيئة من Metro، بينما معاينة Manus أو APK المولد من GitHub قد لا يملك الأسرار نفسها. لذلك يجب مقارنة `EXPO_PUBLIC_SUPABASE_URL` و`EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY` في إعدادات Manus وGitHub، ثم بناء APK جديد.

الملاحظة الثانية هي تعدد المعلمين. قاعدة البيانات تعزل السجلات بالمعرف، لكن تسجيل الدخول الحالي يختار أول معلم فقط. دعم عدة معلمين يحتاج تصميم حسابات صريحاً، وليس مجرد إضافة سجلات إلى `teacher_access`.

## 17. ما ينبغي أن يفعله الذكاء الاصطناعي التالي

يبدأ بقراءة هذه الوثيقة ثم `README.md` و`design.md` و`todo.md`. بعد ذلك يفحص الملفات الفعلية ولا يفترض أن الملفات القديمة هي المسار الحي. قبل تعديل البيانات يراجع `supabase/functions/school-api/index.ts` والترحيلات. عند تعديل التطبيق، يشغل `pnpm check` و`pnpm test`، يختبر عدم الاتصال، ثم يحفظ نسخة يمكن الرجوع إليها.

أي تغيير في الهوية أو كلمات السر أو أسرار Supabase يجب أن يتم عبر مدير الأسرار أو إعدادات GitHub/Supabase الآمنة، وليس بكتابتها في هذه الوثيقة.

---

**نهاية وثيقة التسليم**

تم إعداد هذه الوثيقة لتكون مرجعاً عملياً للمطور التالي، مع تعمد حذف القيم السرية الخام من النسخة القابلة للمشاركة.

## مراجع تقنية

- [Expo Documentation](https://docs.expo.dev/)
- [Expo Router](https://docs.expo.dev/router/introduction/)
- [Supabase Edge Functions](https://supabase.com/docs/guides/functions)
- [GitHub Actions Documentation](https://docs.github.com/actions)
- [React Native Documentation](https://reactnative.dev/docs/getting-started)

## قالب إعداد آمن غير سري

```env
EXPO_PUBLIC_SUPABASE_URL=ضع_عنوان_مشروع_Supabase_هنا
EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=ضع_المفتاح_العام_هنا
```

لا تضف `SUPABASE_SERVICE_ROLE_KEY` أو `SCHOOL_SETUP_KEY` إلى هذا القالب أو إلى تطبيق الهاتف.
